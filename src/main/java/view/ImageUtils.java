package view;

import javax.swing.*;
import java.awt.*;

public class ImageUtils extends JPanel {

    private static final int IMAGE_PANE_SIZE = 700;

    public ImageUtils() {
        setPreferredSize(new Dimension(IMAGE_PANE_SIZE, IMAGE_PANE_SIZE));
    }
}
